---
description: No agent commits — user commits themselves
alwaysApply: true
---

# Commits

**The user commits themselves.** Do not run `git commit` or create commits. When preparing code changes, you may run format and lint and add or run tests; the user will commit when ready.
